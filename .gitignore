# macOS
.DS_Store

# CMake / Ninja build dirs.
build/
build-*/

# Xcode (cpp/ still uses the Xcode generator on macOS)
DerivedData/
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/

# Credentials
.netrc

# Claude Code (per-user settings and local agent scratch, not shared)
.claude/settings.local.json
.claude/worktrees/
